import "./setback-check/registry";
import "./height-check/registry";
import "./sight-corridor/registry";
import "./fire-ladder/registry";
import "./sky-bridge/registry";

// Note: Avoid dynamic require in Next.js app router to prevent
// "Critical dependency" warnings. Keep explicit imports above instead.
